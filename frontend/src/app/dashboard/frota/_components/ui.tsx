"use client";

/**
 * As primitivas nasceram aqui e passaram a servir o sistema inteiro — mudaram
 * para `@/components/data-ui`. Este arquivo é só reexport para não quebrar os
 * imports existentes das páginas de frota.
 */
export * from "@/components/data-ui";
